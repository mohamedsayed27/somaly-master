import 'package:flutter/material.dart';

class TestDirectoryScreen extends StatelessWidget {
  // const TestDirectoryScreen({});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Route Success"),
      ),
    );
  }
}
